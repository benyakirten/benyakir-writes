import * as filter from '@Utils/filter'

describe('filter util', () => {
  describe('createChoiceSet', () => {
    it('should return a set of all items at that key value that are truthy as an object of the expected dimensions', () => {
      // TODO
    })
  })
})
