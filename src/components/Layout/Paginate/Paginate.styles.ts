import styled from 'styled-components'

import { ABS_BLACK, FONT_XL, SHADOW_SM } from '@StyleVars'

export const NoResults = styled.div`
    padding: 2rem 1rem;

    font-size: ${FONT_XL};

    border: 2px solid ${ABS_BLACK};
    border-shadow: ${SHADOW_SM};
`